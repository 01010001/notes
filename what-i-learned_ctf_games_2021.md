## What I learned From CTF Games
### Bandit
i learned:
set up and used the tldr program which is basically man but way shorter
used ssh to connect to remote machines with a specified user and a specified port
used file system navigation commands such as cd, ls to find required files
used cat command to read files
used the ls command with flags to change its behaviour to see hiddens files in a directory
used "./" to specify that the file name in question which has a special character as its first letter is an actual file in the current directory.
used "\\" so that a file with space characters in it are not registered as multiple parameters.
used * special character to specify all the files in a folder.
used du command with flags to list all the files that has the exact size needed.
used the pipe character "|" to pass output from one command to another.
used grep to limit a different programs output to find a specific file.
used the find command with flags to find files which has the exact ownership status.
used grep to find a specific line from a long file.
used the command sort to sort the lines of a long file.
used the command uniq to find the lines that are repeated.
used the strings command to find strings from a messy file.
used the base64 command with flags to find decode a file.
used the tr command with flags to decrypt a file.
used xxd to reverse a hexdump into binary.
used ">>" to add lines at the end of a specified file.
used gzip, bzip, and tar to decompress files.
used a private ssh key to acces a remote machine.
used the telnet command to create a connection with a specified port.
used s_client command to create a connection with a specified port.
used nmap to scan a specified port range for connections.
used nmap with flags to find which ports in a specified range use ssl encryption.
used the chmod command to change the ownership of a file.
used the diff command to find differences between files.
used tmux to have access to multiple session with a singular ssh connection.
wrote a bash script and ran it.
used git to clone a repo.
used git log to see the commit history and then checked the differences between two commits.
used git to change branches.
used git to commit.

### Leviathan:
used the experience from the previous game levels(Bandit).
used the file command to check information about a specific file.
used ltrace command to find passwords of simple c programs.
used ln to create a symbolic link.
wrote a bash script to brute force a simple c program.









